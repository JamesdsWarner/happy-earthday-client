import * as Styled from './MainView.styles';
import EarthdayCounter from './EarthdayCounter/EarthdayCounter.component';

const MainView = () => {
  return <Styled.MainViewWrapper></Styled.MainViewWrapper>;
};

export default MainView;
